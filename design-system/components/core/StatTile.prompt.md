A single stat from the monthly report mock. Four across on desktop, two on mobile — drop tiles rather than shrinking type below 12px.

```jsx
<StatTile label="Lessons attended" value="4/4" />
<StatTile label="Qur'aan · Juz Amma" value="60%" progress={60} tone="gold" />
```

Notes: gold progress is reserved for Qur'aan and other achievement lines. Never invent report figures in marketing surfaces — label mock data as a placeholder.
