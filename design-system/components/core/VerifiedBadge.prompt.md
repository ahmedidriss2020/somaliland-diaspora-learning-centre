The teal verified tick. Show it before the teacher's name — parents buy safety before they buy Somali.

```jsx
<VerifiedBadge size="md" />
<VerifiedBadge state="new" />
```

Notes: teal = verified, gold = newly joined but fully vetted. The tick is never mirrored in RTL. Never use this badge on anything other than a teacher.
